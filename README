ViennaMesh is currently under development, it is considered pre-alpha.

For a glimpse on how it is used right now, look at the test files in tests/
to build the external libraries and the main application(s) issue:

./build.sh


many updates will come the next months, so bear with us ..

 - Josef
