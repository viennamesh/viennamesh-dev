

INCLUDE_DIRECTORIES(".")
INCLUDE_DIRECTORIES("triangle/")

IF(CMAKE_COMPILER_IS_GNUCXX)
   ADD_DEFINITIONS(-O2 -DTRILIBRARY -DLINUX -I/usr/X11R6/include -L/usr/X11R6/lib)
ENDIF(CMAKE_COMPILER_IS_GNUCXX)

ADD_LIBRARY(triangle triangle/triangle.c)