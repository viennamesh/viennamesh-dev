SET(TETGEN_SRC tetgen.cxx predicates.cxx)
ADD_LIBRARY(tetgen ${TETGEN_SRC})
IF(BUILD_SHARED_LIBS)
  SET_TARGET_PROPERTIES(tetgen PROPERTIES COMPILE_FLAGS "-DTETLIBRARY")
ELSE(BUILD_SHARED_LIBS)
  SET_TARGET_PROPERTIES(tetgen PROPERTIES COMPILE_FLAGS "-DTETLIBRARY -fPIC")
ENDIF(BUILD_SHARED_LIBS)

